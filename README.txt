SwitchMap Phase103R10 Dashboard Cards Codex Reviewed Fix

Apply target: C:\SwitchMap
Run only this CMD after extracting the ZIP:

scripts\apply_phase103R10_dashboard_cards_codex_reviewed_fix.cmd

The script creates file backup, applies the payload, verifies, rolls back on failure, and restarts the Scheduled Task only after successful verification.
