Phase 72.4 Nexus Decimal Helper Fix

Scope:
- inventory/management/commands/sfp_background_monitor.py

Fix:
- Adds missing _to_decimal helper for NX-OS transceiver parsing.
- No credential, UI, DB schema, task, search, or topology changes.
