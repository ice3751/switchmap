# Phase 42.2
Fixes dashboard Quick Search after the dashboard was changed to a collapsed minimal device browser.

- Opens the device browser automatically when a query exists.
- Filters switch cards by all search terms.
- Shows result count and empty state.
- Keeps behavior dynamic for new switches because it uses data-search/text from every switch card.
