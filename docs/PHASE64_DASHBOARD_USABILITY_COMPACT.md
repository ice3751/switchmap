# Phase 64 - Dashboard Usability Compact

Goal: reduce dashboard clutter without changing data collection logic.

Changes:
- Compact dashboard cards and panels.
- Reduce empty whitespace in notifications.
- Keep action items scrollable instead of stretching the whole page.
- Keep raw/device data collapsed.
- Keep background refresh task installed and run once after patch.

No database migration.
No device configuration changes.
No credential changes.
